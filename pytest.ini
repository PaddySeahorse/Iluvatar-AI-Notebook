[pytest]
testpaths = tests
pythonpath = .
markers =
    integration: end-to-end tests that spawn a real ipykernel subprocess (slower)
    iluvatar: tests that require Iluvatar GPU hardware
    e2e: browser-driven end-to-end tests (Playwright)
